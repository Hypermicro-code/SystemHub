import React from "react";

export function TableCore() {
  return (
    <div
      style={{
        flex: 1,
        background: "var(--mc-table-bg)",
        color: "var(--mc-table-fg)",
        display: "grid",
        placeItems: "center",
      }}
    >
      (TableCore placeholder)
    </div>
  );
}
